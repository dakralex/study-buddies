import React from 'react';
import {View} from 'react-native';
import {ProfileScreenProps as Props} from '../../navigation/types';

const ProfileScreen = ({}: Props) => {
  return <View />;
};

export default ProfileScreen;
