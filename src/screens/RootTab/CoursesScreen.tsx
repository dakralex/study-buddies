import React from 'react';
import {View} from 'react-native';
import {CoursesScreenProps as Props} from '../../navigation/types';

const CoursesScreen = ({}: Props) => {
  // TODO This is a stub for a CoursesStack or some similar navigation inside the Courses tab
  return <View />;
};

export default CoursesScreen;
