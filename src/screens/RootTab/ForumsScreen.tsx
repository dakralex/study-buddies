import React from 'react';
import {View} from 'react-native';
import {ForumsScreenProps as Props} from '../../navigation/types';

const ForumsScreen = ({}: Props) => {
  // TODO This is a stub for a ForumsStack or some similar navigation inside the Forums tab
  return <View />;
};

export default ForumsScreen;
